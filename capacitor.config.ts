import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jkhhuse.plan',
  appName: 'plan-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
